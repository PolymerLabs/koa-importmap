{
  "name": "koa-importmap",
  "version": "1.0.0",
  "description": "A Koa middleware module that can rewrite import specifiers for JavaScript modules based on the reference/presence of an importmap in the HTML document originating the request.",
  "main": "index.js",
  "scripts": {
    "test": "tape test/* | tap-diff"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parse5": "^5.1.0",
    "parse5-traverse": "^1.0.3"
  },
  "devDependencies": {
    "standard": "^12.0.1",
    "tap-diff": "^0.1.1",
    "tape": "^4.10.1"
  }
}
